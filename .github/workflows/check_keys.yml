name: Check VLESS Keys

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

concurrency:
  group: check-keys
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run checker
        run: python check_and_save.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Используем -f чтобы обойти .gitignore
          git add -f docs/keys.json
          git add -f docs/working_keys.txt
          git commit -m "chore: update keys and txt list [skip ci]" || echo "No changes to commit"
          git pull --rebase origin main
          git push
