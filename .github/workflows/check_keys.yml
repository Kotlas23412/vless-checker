name: Check VLESS Keys

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

concurrency:
  group: check-keys
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run checker
        run: python check_and_save.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Добавляем и JSON и TXT файлы
          git add docs/keys.json
          git add docs/working_keys.txt
          # Фиксируем изменения, если они есть
          git diff --cached --quiet || git commit -m "chore: update keys and txt list [skip ci]"
          git pull --rebase origin main
          git push
